# 实施计划

> 本文档记录项目 v1.0.0 内部测试版的历史实施路线，并在后半部分补充后续功能优化的工程执行顺序。产品级修改说明保留在 `memory-bank/modification-plan.md`，本文档用于指导后续按 Step 开发、验收和更新进度。

## 阶段一：项目初始化（Step 1-3，已完成）

### Step 1: 创建 Next.js 项目骨架
**任务**：
- 使用 create-next-app 初始化项目
- 配置 TypeScript + Tailwind CSS
- 安装 shadcn/ui 基础组件：Button、Input、Card、Dialog、Select、Form、Dropdown Menu、Toast、Sheet

**验证方式**：
- 本地运行 `npm run dev`，浏览器访问 http://localhost:3000 能看到默认页面
- 检查页面无报错

---

### Step 2: Firebase 项目配置
**任务**：
- 登录 Firebase 控制台，创建项目
- 开启 Authentication（邮箱密码登录）
- 开启 Firestore 数据库
- 获取项目配置信息（apiKey 等）

**验证方式**：
- 通过开发的 App 注册测试用户
- 注册后能在 Firestore 看到用户数据

---

### Step 3: Firebase 集成
**任务**：
- 在项目中安装 firebase SDK
- 创建 firebase.ts 初始化文件
- 封装 Auth 和 Firestore 工具函数

**验证方式**：
- 登录页面能正常调用 Firebase 登录/注册 API
- 新用户数据能写入 Firestore

---

## 阶段二：核心页面搭建（Step 4-6，已完成）

### Step 4: 登录/注册页面
**任务**：
- 创建登录页面（邮箱、密码登录）
- 创建注册页面
- 添加表单验证（邮箱格式、密码长度）

**验证方式**：
- 用测试邮箱能成功注册
- 注册后自动跳转到仪表盘
- 刷新页面保持登录状态

---

### Step 5: 首页仪表盘
**任务**：
- 创建布局框架（顶部导航、侧边栏）
- 显示用户今日体重简报
- 展示快速打卡入口（记录体重/饮食/运动）
- 显示今日复盘入口

**验证方式**：
- 登录后进入首页能看到布局
- 各入口按钮可点击

---

### Step 6: 目标设定页面
**任务**：
- 创建目标设定表单
- 输入：当前体重、目标体重、目标日期
- 计算预估周期是否合理，显示提示

**验证方式**：
- 填写后数据保存到 Firestore
- 再次进入页面能读取已保存的目标

---

## 阶段三：数据记录模块（Step 7-10，已完成）

### Step 7: 体重记录
**任务**：
- 创建体重记录页面
- 支持多次记录（晨起体重为主值）
- 记录时间、是否晨起
- 显示周趋势图、月趋势图

**验证方式**：
- 记录一条体重后能在趋势图显示
- 趋势图随数据更新

---

### Step 8: 围度记录
**任务**：
- 创建围度记录页面
- 支持记录：腰围、臀围、大腿围、上臂围
- 显示维度趋势图（与体重趋势并列）

**验证方式**：
- 记录后能在趋势图显示
- 支持按周/按月查看

---

### Step 9: 饮食记录
**任务**：
- 创建饮食记录页面
- 六时段：早餐、午餐、晚餐、上午加餐、下午加餐、晚上加餐
- 每时段记录：食物描述、份量、饥饿评分（1-5）、触发原因、情绪状态、吃后感受
- 触发原因体系（生理饥饿、口欲、社交、压力等）
- 情绪触发源（工作压力、人际冲突等）

**验证方式**：
- 能为一天添加多时段饮食记录
- 填写各字段后能保存成功

---

### Step 10: 运动记录
**任务**：
- 创建运动记录页面
- 记录：运动类型、时长、消耗卡路里、强度（轻/中/高）
- 支持每日多条

**验证方式**：
- 记录后能在记录列表显示

---

## 阶段四：复盘系统（Step 11-13，部分实现）

### Step 11: 日复盘
**任务**：
- 在饮食记录界面添加复盘入口
- 自动生成日复盘内容
- 显示：今日执行度、触发性进食次数、做得好的1件事、明日优先改进1件事
- 以上全部由系统分析当日数据自动生成

**验证方式**：
- 一天的饮食记录完成后能看到日复盘
- 复盘内容根据记录数据自动生成

---

### Step 12: 周复盘
**任务**：
- 饮食记录界面包含日复盘和周复盘两个 Tab
- 自动生成周复盘内容
- 显示：体重周均变化、围度变化、饮食结构概览
- 分析：情绪-进食关联、高风险场景TOP3
- 输出：下周3条可执行策略（系统自动推荐）
- 后续版本：根据用户历史数据（加餐原因、情绪等）综合分析

**验证方式**：
- 一周的记录完成后能在饮食记录页看到周复盘
- 高风险场景分析逻辑正确
- 系统能自动推荐可执行策略

**当前说明**：
- 独立 `/dashboard/review` 页面尚未实现，已列入 `modification-plan.md` 的第一阶段。

---

### Step 13: 复盘联动计划
**任务**：
- 周复盘结束后一键生成下周饮食草案
- 提前布局高风险时段加餐
- 降低触发食物暴露

**验证方式**：
- 能根据周复盘数据生成下周计划

---

## 阶段五：菜谱生成（Step 14-16，已完成基础版）

### Step 14: 食材库存输入
**任务**：
- 创建食材库存管理页面
- 输入：食材名称、数量、剩余天数
- 支持食材分类（肉类、主食、蔬菜等）
- 用户通过此页面输入食材后，系统读取库存生成菜谱

**验证方式**：
- 添加食材后能在列表显示
- 能编辑和删除食材

---

### Step 15: 菜谱生成（基础版）
**任务**：
- 读取 Step 14 的食材库存
- 根据食材生成一周每日三餐安排
- 食材消耗节奏规划（防浪费）
- 简单健康做法推荐

**验证方式**：
- 输入库存后能生成一周菜单
- 菜单能显示每餐搭配

---

### Step 16: 菜谱生成（增强版）
**任务**：
- 添加条件约束引擎
- 场景选择：宿舍/出租屋/家庭
- 设备选择：电饭煲/微波炉/空气炸锅等
- 时间约束：每餐可投入时长
- 根据用户历史习惯（易饿时段、偏好做法）调整菜单
- 用户使用一周后，学习历史数据（加餐原因、情绪等）综合推荐

**验证方式**：
- 设置条件后生成的菜单符合约束
- 不同用户能看到不同推荐

---

## 阶段六：优化与上线（Step 17-19，已完成）

### Step 17: 响应式适配
**任务**：
- 优化移动端体验
- 检查各页面在手机上的显示效果
- 修复布局问题

**验证方式**：
- 用手机浏览器访问，能正常操作
- 按钮、输入框等可点击

---

### Step 18: 用户体验优化
**任务**：
- 添加首次使用引导
- 页面内提示说明
- 优化加载速度
- 添加加载状态

**验证方式**：
- 首次用户能顺利完成目标设置
- 页面切换流畅

---

### Step 19: 部署上线
**任务**：
- 推送代码到 GitHub
- 配置 Vercel 自动部署
- 验证生产环境可访问

**验证方式**：
- 访问 Vercel 生成的 URL 能正常使用
- 数据能正常读写

---

## 阶段七：核心问题修复（Step 20-23，进行中）

### Step 20: 仪表盘体重同步与文案调整 ✅
**任务目标**：
- 修复已录入今日体重后，仪表盘“今日体重”未同步显示的问题。
- 统一“初始体重、今日/最新体重、目标体重”的产品口径。
- 让进展概览基于最新体重动态计算剩余差距。

**修改范围**：
- 仪表盘今日体重改为读取当天体重记录子集合。
- 今日体重优先取晨起体重；若无晨起体重，则取当天最新体重。
- 将目标设定中的“当前体重”展示文案调整为“初始体重”。
- 进展概览使用今日体重或最近一次体重与目标体重计算。
- 今天没有体重记录时，使用最近一次体重作为参考，并在界面标注。

**验证方式**：
- 在体重记录页新增今日体重后，回到仪表盘能立即看到。
- 仪表盘显示“今日体重、初始体重、目标体重”三项。
- 进展概览的“还需”随最新体重变化。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若调整体重数据读取口径，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- 内部字段 `currentWeight` 保持不变，界面口径改为“初始体重”。
- 今日体重读取 `weight` 子集合，优先晨起体重，否则当天最新记录；今日无记录时回看最近 90 天并标注“最近记录”。

---

### Step 21: 新建独立复盘页面 ✅
**任务目标**：
- 修复导航和首页快速入口跳转 `/dashboard/review` 时出现 404 的问题。
- 将复盘能力从饮食记录页中独立出来，形成明确的核心功能入口。

**修改范围**：
- 新建 `/dashboard/review` 页面。
- 复用或迁移现有日复盘、周复盘逻辑。
- 页面展示今日复盘和本周复盘。
- 无记录时展示空状态和引导入口。
- 饮食记录页可保留轻量复盘入口，完整复盘以独立页面为主。

**验证方式**：
- 点击侧边栏“复盘”不再 404。
- 点击首页复盘入口能进入独立复盘页面。
- 有记录时能展示今日复盘和本周复盘。
- 无记录时能看到明确空状态和记录引导。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若页面结构或导航结构调整，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- 新增 `/dashboard/review` 独立复盘页面。
- 新增 `src/lib/review.ts` 复盘分析模块，饮食页和独立复盘页共用日/周复盘逻辑。
- 无记录时展示空状态，并引导用户前往饮食记录页。

---

### Step 22: 围度记录移动端优化与删除能力 ✅
**任务目标**：
- 降低手机端围度摘要卡片的纵向占用。
- 支持删除误录的围度数据。

**修改范围**：
- 移动端围度摘要卡片改为两列展示。
- 压缩卡片文案和字号，减少纵向高度。
- 为围度记录增加删除能力。
- 优先支持删除今日记录；如需删除任意历史记录，再扩展历史列表的数据结构。
- 删除后同步刷新今日记录和趋势图。

**验证方式**：
- 手机端围度摘要不再单列过长。
- 今日围度记录列表中可以删除误录数据。
- 删除后今日记录和趋势图同步刷新。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若新增或调整围度记录删除相关数据结构，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- 围度摘要卡片在移动端改为两列紧凑展示，并压缩卡片文案、字号和间距。
- 今日围度记录支持删除误录数据，删除前有确认弹窗。
- 删除后同步刷新今日记录和趋势图。
- 当前仅支持删除今日围度记录，未扩展任意历史记录删除。
- 新增通用 `deleteDailyRecord` 方法，未调整 Firestore 数据结构。

---

### Step 23: 运动记录支持自定义运动量单位 ✅
**任务目标**：
- 将运动记录从固定“时长/分钟”扩展为可表达步数、公里数、次数等运动量。
- 保持旧数据兼容显示。

**修改范围**：
- 将运动记录字段从单一 `duration` 扩展为“运动量数值 + 单位”。
- 单位支持：分钟、步、公里、次、组、圈、自定义。
- 卡路里字段保持可选。
- 运动记录列表显示用户选择的单位。
- 今日统计从“运动时长”调整为更通用的“运动量记录”，或按单位分组汇总。
- 旧数据兼容显示为“分钟”。

**验证方式**：
- 用户可以记录“走路 8000 步”。
- 用户仍可以记录“跑步 30 分钟”。
- 旧运动记录能继续显示为分钟单位。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中运动记录数据结构说明。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- 运动记录从固定 `duration` 扩展为 `amount`、`unit`、`customUnit`，支持分钟、步、公里、次、组、圈和自定义单位。
- 新记录选择“分钟”时继续写入 `duration`，用于兼容旧字段和分钟类记录。
- 旧运动记录没有 `amount`、`unit` 时按 `duration + 分钟` 兼容显示。
- 今日统计从“运动时长”调整为按单位分组汇总的“运动量记录”。
- 测试过程中修复登录成功后页面在首页、仪表盘和登录页之间反复跳转的问题。

---

## 阶段八：页面合并与信息架构优化（Step 24-25，待执行）

### Step 24: 合并目标设定和体重记录 ✅
**任务目标**：
- 将目标设定和体重记录合并为同一任务流，减少导航割裂。
- 统一仪表盘和体重页对体重相关指标的展示口径。

**修改范围**：
- 将“目标设定”整合进“体重记录”页面。
- 页面顶部展示目标摘要：初始体重、目标体重、今日/最新体重、剩余差距。
- 页面中部保留快速记录今日体重。
- 页面下部展示趋势图、历史记录、目标设置表单。
- 侧边栏移除独立“目标设定”入口，或改为跳转到体重页面的目标区域。

**验证方式**：
- 一个页面内可以完成目标设置和体重记录。
- 仪表盘、体重页对“初始体重/今日体重/目标体重”的口径一致。
- 原目标设定入口不会造成 404 或导航断点。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中页面结构和导航说明。
- 必要时同步更新 `memory-bank/modification-plan.md` 的任务状态。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- 体重记录页顶部新增目标摘要，显示今日/最新体重、初始体重、目标体重和剩余差距。
- 体重记录页中部保留快速记录今日体重，下部保留趋势图和今日记录，并新增目标设置表单。
- 侧边栏移除独立“目标设定”入口。
- 旧 `/dashboard/goal` 路由保留为兼容跳转页，自动跳转到 `/dashboard/weight#goal-settings`，避免导航断点。
- 仪表盘空状态文案改为引导用户到体重记录中设置体重目标。

---

### Step 25: 合并食材库存和菜谱生成 ✅
**任务目标**：
- 将强依赖食材库存的菜谱生成能力合并到同一页面，减少来回切换。
- 让菜谱生成即时读取最新库存。

**修改范围**：
- 新页面可命名为“食材与菜谱”。
- 页面内使用 Tab 或分区组织：
  - 食材库存
  - 一周菜谱
  - 条件设置
- 侧边栏减少为一个相关入口。
- 添加食材后，菜谱生成读取最新库存数据。

**验证方式**：
- 用户在同一页面添加食材后，可以直接生成菜谱。
- 菜谱生成能即时读取最新库存。
- 原食材库存和菜谱入口不会造成 404 或导航断点。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中页面结构和菜谱生成流程说明。
- 必要时同步更新 `memory-bank/modification-plan.md` 的任务状态。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 用户验证测试通过。
- `/dashboard/inventory` 合并为“食材与菜谱”页面，使用 Tab 组织食材库存、一周菜谱和条件设置。
- 侧边栏保留一个“食材与菜谱”入口，移除独立菜谱生成入口。
- 添加、编辑、删除食材后，同页菜谱生成读取最新库存状态。
- 旧 `/dashboard/recipe` 路由保留为兼容跳转页，自动跳转到 `/dashboard/inventory?tab=recipe`，避免导航断点。
- 合并后全量 `npm run lint` 无 error，仅剩既有 warning。

---

## 阶段九：菜谱生成升级（Step 26-28 已完成）

### Step 26: 菜谱展示压缩为表格/日历视图 ✅
**任务目标**：
- 解决一周菜谱篇幅过长、不便一眼查看的问题。
- 提供桌面端和移动端都可快速浏览的一周三餐视图。

**修改范围**：
- 桌面端使用一周表格：日期、早餐、午餐、晚餐。
- 移动端使用紧凑日期卡片或横向表格。
- 支持视图切换：
  - 表格视图
  - 九宫格视图
  - 日历视图

**验证方式**：
- 一周三餐可以在较短页面内完整查看。
- 手机端不需要大量滚动才能看完主要内容。
- 视图切换后菜谱内容保持一致。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若菜谱组件结构变化较大，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 一周菜单支持表格视图、九宫格视图和日历视图切换。
- 桌面端表格按日期、早餐、午餐、晚餐展示；移动端表格视图使用紧凑日期卡片。
- 三种视图共用同一份菜谱数据，切换后内容保持一致。
- `npx eslint src/app/dashboard/inventory/page.tsx` 通过。
- `npx tsc --noEmit` 通过。

---

### Step 27: 菜谱导出为便于查看的一页纸 ✅
**任务目标**：
- 支持用户将一周菜谱导出为便于日常查看的一页纸。
- 优先使用浏览器打印或保存 PDF，避免引入不必要的复杂导出链路。

**修改范围**：
- 添加菜谱导出入口。
- 提供专用打印样式。
- 打印模板支持：
  - 一页纸表格
  - 九宫格
  - 日历形式
- 打印内容隐藏无关按钮、导航和表单。

**验证方式**：
- 用户点击导出后，可以通过浏览器保存为 PDF。
- 打印预览中不包含无关按钮、导航和表单。
- 一页纸内容排版清晰，不截断核心菜谱信息。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若新增导出组件或打印样式约定，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-25。
- 一周菜单区域新增“导出 / 保存 PDF”入口，直接调用浏览器打印能力。
- 打印内容跟随当前视图，且仅导出当前一周菜单；支持导出表格、九宫格和日历三种形式。
- 打印时隐藏侧边栏、顶部栏、移动端菜单按钮和页面内无关按钮、表单。
- 打印样式默认使用 A4 横向紧凑布局，保留菜谱标题、日期范围和菜单内容。
- 打印范围采用菜谱容器白名单控制，避免将库存、食材使用计划和条件设置带入导出内容。
- `npx eslint src/app/dashboard/inventory/page.tsx src/app/dashboard/layout.tsx` 通过。
- `npx tsc --noEmit` 通过。

---

### Step 28: 菜谱生成规则和库存不足提示 ✅
**任务目标**：
- 让菜谱生成遵循更明确的搭配规则。
- 避免生成用户没有录入的食材。
- 在库存不足时给出明确提示和补货方向。

**修改范围**：
- 生成菜谱时只使用用户库存中的食材。
- 建立基础搭配规则：
  - 早餐：主食 + 蛋白/蛋奶，可选水果。
  - 午餐：2 份蔬菜 + 1 份蛋白质 + 1 份主食。
  - 晚餐：蔬菜 + 蛋白质，主食可选。
- 库存不足时，不强行生成完整一周菜单。
- 输出补货清单，例如：蛋白质不足、蔬菜不足、主食不足。

**验证方式**：
- 菜谱不会默认出现用户未录入的食材。
- 库存不足时，页面明确提示“当前库存不足以生成完整一周菜谱”。
- 页面能给出需要补充的食材类别。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中菜谱生成规则、库存约束和补货提示逻辑。
- 必要时同步更新 `memory-bank/modification-plan.md` 的任务状态。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 菜谱生成改为只使用用户库存中的食材，不再使用内置推荐食材兜底。
- 三餐搭配规则已落地：早餐主食 + 蛋白/蛋奶 + 可选水果；午餐 2 份蔬菜 + 蛋白质 + 主食；晚餐蔬菜 + 蛋白质 + 可选主食。
- 肉类和蛋奶作为蛋白质来源，主食、蔬菜、蛋白质按临期优先轮换使用。
- 库存不足时不生成完整一周菜单，并显示“当前库存不足以生成完整一周菜谱”和补货建议。
- 水果缺失只作为可选补充建议，不阻断生成。
- `npx eslint src/app/dashboard/inventory/page.tsx` 通过。
- `npx tsc --noEmit` 通过。
- `npm run lint` 无 error，仅剩既有 warning。

---

## 总览

| 阶段 | 步骤 | 内容 |
|---|---|---|
| 一 | 1-3 | 项目初始化 + Firebase 配置 |
| 二 | 4-6 | 登录页面 + 仪表盘占位 + 目标设定 |
| 三 | 7-10 | 体重/围度/饮食/运动记录 |
| 四 | 11-13 | 日复盘 + 周复盘 + 计划联动 |
| 五 | 14-16 | 食材库存 + 菜谱生成 |
| 六 | 17-19 | 响应式 + 体验优化 + 上线 |
| 七 | 20-23 | 核心问题修复：仪表盘体重同步、独立复盘页、围度移动端与删除、运动单位自定义 |
| 八 | 24-25 | 页面合并：目标设定 + 体重记录、食材库存 + 菜谱生成 |
| 九 | 26-28 | 菜谱升级：表格/日历视图、导出、211 搭配规则和库存不足提示 |
| 十 | 29-31 | 入口体验与视觉升级：未登录只读浏览、冰蓝玻璃感和真实记录提示已完成 |
| 十一 | 32-36 | 个人资料与记录体验：昵称、体重目标折叠、围度最近值、饮食时间、仪表盘围度 |
| 十二 | 37-38 | 库存顿数与菜谱消耗 |
| 十三 | 39-40 | 身体资料、BMI 与轻量营养目标 |

---

## 阶段十：入口体验、视觉风格与记录可信度（Step 29-31 已完成）

### Step 29: 未登录用户完整只读浏览 ✅
**任务目标**：
- 降低新用户、招聘方和开源访问者的首次浏览门槛。
- 允许未登录用户先查看仪表盘和各功能页面，再在写入时登录或注册。

**修改范围**：
- 调整 dashboard 鉴权守卫，未登录不再强制跳转 `/login`。
- 未登录用户可访问 `/dashboard` 和各 dashboard 子页面。
- 页面在未登录状态下展示只读空状态、功能说明或示例状态。
- 所有写入动作统一拦截，包括记录、保存、删除、生成个人计划、编辑资料等。
- 未登录点击写入动作时弹出登录/注册提醒。

**验证方式**：
- 未登录访问 `/`、`/dashboard` 和各 dashboard 子页面不会自动跳转登录。
- 未登录点击任一写入动作时看到登录/注册提醒。
- 未登录状态不会写入 Firestore。
- 已登录用户原有记录流程正常。

**影响文档**：
- 已更新 `memory-bank/progress.md`。
- 已更新 `memory-bank/architecture.md` 中的访问权限和鉴权流程说明。

---

### Step 30: 冰蓝玻璃感视觉升级 ✅
**任务目标**：
- 将界面从黑白灰后台模板感升级为偏冷色、时尚、个性的视觉风格。
- 保持记录工具的可读性和信息密度。

**修改范围**：
- 建立冷白、浅蓝灰、蓝青色主基调。
- 调整 dashboard shell、侧边栏、顶部栏、Card、Button、Tabs、Input 等基础视觉。
- 使用半透明卡片、细边框和柔和阴影增强层次。
- 保持打印样式和表单可用性。

**验证方式**：
- 页面整体不再呈现单一黑白灰后台感。
- 桌面端和移动端主要页面保持清晰可读。
- 菜谱打印导出不被视觉样式破坏。
- `npx tsc --noEmit` 通过。
- `npm run lint` 无 error，仅剩既有 warning。
- `npm run build` 通过。
- 用户网页端验收通过。

**影响文档**：
- 已更新 `memory-bank/progress.md`。
- 已更新 `memory-bank/architecture.md` 中的全局视觉约定。

---

### Step 31: 真实记录原则提示 ✅
**任务目标**：
- 强化记录用于复盘的产品原则。
- 明确历史记录暂不支持修改。

**修改范围**：
- 首页和体重、围度、饮食、运动等记录页增加统一提示。
- 文案表达：记录用于复盘，请尽量真实填写；历史记录暂不支持修改。
- 今日误录删除或重记能力保留，不扩展任意历史编辑。

**验证方式**：
- 首页和各记录页能看到真实记录提示。
- 历史记录不可修改原则表达清楚。

**影响文档**：
- 已更新 `memory-bank/progress.md`。
- 已更新 `memory-bank/architecture.md` 中的共享提示组件说明。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 用户验证测试通过。
- 新增统一 `RecordPrincipleNotice` 组件，首页、仪表盘和体重、围度、饮食、运动记录页共用同一提示。
- 提示文案明确记录用于复盘、请尽量真实填写、历史记录暂不支持修改。
- 保留今日误录可按页面已有能力删除或重新记录的说明，不扩展任意历史编辑能力。

---

## 阶段十一：个人资料与记录体验优化（Step 32-36，进行中）

### Step 32: 昵称与个人资料 ✅
**任务目标**：
- 让顶部栏和欢迎语从邮箱展示升级为更自然的昵称展示。
- 为后续身体指标和营养目标预留基础资料。

**修改范围**：
- 用户资料支持昵称。
- 顶部栏和仪表盘欢迎语优先显示昵称，未设置时回退邮箱。
- 用户资料预留身高、性别、出生年份或年龄、活动水平字段。

**验证方式**：
- 用户可以保存昵称。
- 顶部栏和仪表盘欢迎语显示昵称。
- 旧用户未设置昵称时继续显示邮箱。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中用户资料字段说明。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 用户验证测试通过。
- 新增 `/dashboard/profile` 个人资料页，支持保存昵称、身高、性别、出生年份和活动水平。
- 顶部栏和仪表盘欢迎语优先显示昵称，未设置昵称时回退邮箱。
- 右上角昵称和头像区域可点击跳转到个人资料页。
- 侧边栏新增“个人资料”入口，未登录状态可只读浏览，写入时弹出登录/注册提醒。
- `UserProfile` 和 `getUserProfile` 已扩展新增字段，旧用户未设置昵称或基础资料时继续兼容。
- `npx tsc --noEmit` 通过。
- `npm run lint` 无 error，仅剩既有 warning。
- `npm run build` 通过。

---

### Step 33: 体重目标顶部折叠与 7 天趋势修正 ✅
**任务目标**：
- 优化体重页任务流。
- 修正 7 天趋势只显示有记录日期的问题。

**修改范围**：
- 体重目标摘要放到页面顶部。
- 使用“设置目标”按钮展开目标表单，默认折叠。
- 7 天趋势固定显示今天及过去 6 天日期刻度。
- 缺失日期不画点、不错误连线。

**验证方式**：
- 进入体重页即可看到目标摘要和设置目标入口。
- 点击“设置目标”能展开并保存目标。
- 7 天趋势始终显示 7 个日期刻度。
- 缺失数据不会显示为 0 或错误连线。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若调整体重历史数据展示口径，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 用户验证测试通过。
- 体重目标区域已移到顶部摘要卡片下方，表单默认折叠，通过“设置目标”按钮展开或收起。
- 7 天趋势固定显示今天和过去 6 天日期刻度，缺失日期使用空值，不显示为 0，也不跨缺失日期错误连线。
- 30 天趋势保留按已有记录展示。
- 同步修复 dashboard 布局登录态兜底读取造成的 hydration mismatch。
- `npx tsc --noEmit` 通过。
- `npm run lint` 无 error，仅剩既有 warning。
- `npm run build` 通过。

---

### Step 34: 围度最近有效记录展示 ✅
**任务目标**：
- 避免今天没记录围度时摘要误显示为空。
- 区分未记录和记录值为 0 的语义。

**修改范围**：
- 围度顶部 4 个摘要显示最近一次有效围度记录。
- 摘要标注最近记录日期。
- 单项无有效记录时显示“未记录”，不使用 0。

**验证方式**：
- 今天没有围度记录但历史有记录时，摘要显示最近记录值和日期。
- 单项缺失不会显示 0 cm。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 若调整围度读取逻辑，更新 `memory-bank/architecture.md`。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 用户验证测试通过。
- 围度顶部 4 个摘要已改为分别显示最近 90 天内的最近有效记录，并标注记录日期。
- 单项没有有效记录时显示“未记录”，不再显示 `0 cm`。
- 新增 `getLatestMeasurementSummary` 读取方法，按字段独立查找最近有效值。
- 围度变化计算只比较有效记录，避免空字段或 0 值参与计算。
- 重构围度页加载逻辑，消除该页面原有 hook dependency warning。
- `npx tsc --noEmit` 通过。
- `npm run lint` 无 error，仅剩既有饮食页 warning。
- `npm run build` 通过。

---

### Step 35: 饮食记录增加进食时间
**任务目标**：
- 为后续高风险时段分析提供数据。
- 区分“餐次”和“实际进食时间”。

**修改范围**：
- 饮食记录表单增加进食时间，默认当前时间。
- 保存饮食记录时写入进食时间。
- 今日记录列表显示进食时间。
- 旧记录没有进食时间时兼容显示创建时间或空状态。

**验证方式**：
- 用户可以记录具体进食时间。
- 今日记录列表能显示进食时间。
- 旧记录兼容显示正常。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中饮食记录字段说明。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 用户验证测试通过。
- 饮食记录表单已新增进食时间，默认填入当前时间。
- 保存饮食记录时写入 `mealTime`，并保留餐次 `mealType`。
- 今日记录列表优先显示进食时间；旧记录没有 `mealTime` 时回退显示创建时间。
- 扩展 `FoodRecord` 和 `FoodReviewRecord` 类型，周复盘读取继续兼容新旧记录。
- 重构饮食页加载逻辑，消除该页面原有 hook dependency warning。
- `npx tsc --noEmit` 通过。
- `npm run lint` 通过，无 warning。
- `npm run build` 通过。
- 本地已有 `npm run dev -- --port 3001` 服务，访问 `/dashboard` 返回 `200 OK`。

---

### Step 36: 仪表盘增加围度概览
**任务目标**：
- 让仪表盘不仅展示体重，也展示身体围度变化。

**修改范围**：
- 仪表盘增加最近围度概览。
- 显示腰围、臀围、大腿围、上臂围最近有效值和记录日期。
- 优先突出腰围。

**验证方式**：
- 仪表盘能展示最近围度数据。
- 无围度记录时显示明确空状态。
- 最近围度日期标注清楚。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中仪表盘展示内容说明。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 仪表盘已新增围度概览，显示腰围、臀围、大腿围、上臂围最近有效值和记录日期。
- 腰围使用重点卡片展示，其他围度使用紧凑网格展示。
- 无围度记录时显示明确空状态。
- 复用已有 `getLatestMeasurementSummary`，不新增 Firestore 字段。
- `npx tsc --noEmit` 通过。
- `npm run lint` 通过，无 warning。
- `npm run build` 通过。

---

## 阶段十二：库存顿数与菜谱消耗升级（Step 37 已完成，Step 38 待执行）

### Step 37: 食材库存支持可用顿数
**任务目标**：
- 让库存记录贴近日常食材使用方式。
- 支持“这份食材计划吃几顿”的表达。

**修改范围**：
- 食材库存支持可用顿数字段。
- 用户可以继续记录重量/数量，也可以补充可用顿数。
- 食材列表展示剩余数量和可用顿数。
- 旧食材没有可用顿数字段时正常兼容。

**验证方式**：
- 用户可以为食材填写可用顿数。
- 旧食材正常展示和编辑。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中食材库存字段说明。

**完成说明**：
- 已完成，完成日期：2026-04-26。
- 食材库存新增可选 `servings` 字段，用于记录可用顿数。
- 添加食材表单和编辑弹窗均支持填写、修改或清空可用顿数。
- 食材列表和一周菜谱页库存卡片展示剩余数量和可用顿数。
- 旧食材没有可用顿数字段时显示“可用顿数未填”，不迁移历史数据。
- 本次不修改菜谱生成消耗逻辑，按顿数消耗留到 Step 38。
- `npx tsc --noEmit` 通过。
- `npm run lint` 通过，无 warning。
- `npm run build` 通过。
- 本地已有 `npm run dev -- --port 3001` 服务，访问 `/dashboard/inventory` 返回 `200 OK`。

---

### Step 38: 菜谱生成按顿数消耗库存
**任务目标**：
- 避免菜谱生成无限重复安排同一食材。
- 让库存不足提示结合可用顿数更准确。

**修改范围**：
- 生成菜谱时优先根据可用顿数安排食材。
- 每安排一次食材，占用 1 顿。
- 不超过可用顿数重复安排同一食材。
- 库存不足提示结合可用顿数计算补货方向。

**验证方式**：
- 可用顿数为 4 的食材最多被安排 4 次。
- 顿数不足时页面提示对应类别不足。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中菜谱生成库存约束说明。

---

## 阶段十三：身体资料与轻量营养目标（Step 39-40，待执行）

### Step 39: 身体基础资料与 BMI
**任务目标**：
- 为后续营养目标估算提供基础数据。
- 在仪表盘展示更完整的身体概览。

**修改范围**：
- 个人资料增加身高、性别、出生年份或年龄、活动水平。
- 根据身高和体重计算 BMI。
- 仪表盘展示 BMI 和基础身体概览。

**验证方式**：
- 用户可以保存身体基础资料。
- 仪表盘能显示 BMI。
- 缺少必要资料时显示引导补充。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中用户资料和仪表盘说明。

---

### Step 40: 轻量营养目标与菜谱份量建议
**任务目标**：
- 让菜谱结合身体资料给出更合理的参考份量。
- 避免医疗级饮食处方表达。

**修改范围**：
- 基于身高、体重、性别、年龄、活动水平估算基础代谢和每日热量范围。
- 给出三大营养素参考目标。
- 菜谱生成展示每餐参考份量和与目标的粗略匹配。
- 页面文案使用“参考建议”，避免“科学处方”“医疗建议”等强承诺。

**验证方式**：
- 用户能看到每日热量和三大营养素参考目标。
- 菜谱能展示每餐参考份量。
- 页面明确说明结果仅供个人记录和参考。

**影响文档**：
- 完成后更新 `memory-bank/progress.md`。
- 更新 `memory-bank/architecture.md` 中营养目标和菜谱份量建议说明。

## 后续执行与记录规则

当前内部测试后提出的新一轮产品修改计划见：

- `memory-bank/modification-plan.md`

执行规则：

- Step 22-28 已按顺序完成并记录完成说明。
- 后续功能开发按新的产品反馈继续拆分，除非遇到明显依赖调整。
- 每完成一个 Step，同步更新 `memory-bank/progress.md`，记录完成内容、验证结果和遗留问题。
- 如果修改页面结构、导航结构、数据结构或核心流程，同步更新 `memory-bank/architecture.md`。
- `memory-bank/modification-plan.md` 保留为产品级修改说明；本文档作为工程执行顺序说明。
