# 好食家

好食家是一个个人减脂记录与复盘网页应用，用来记录体重、围度、饮食、运动和食材库存，并基于日常记录生成复盘与一周菜谱建议。

这个项目来自我的个人使用需求：减脂过程中，仅记录数据并不足够，真正有价值的是从记录中发现行为模式，并把复盘结果转化为下一步可以执行的调整。

## 在线体验

- 线上地址：[好食家](https://haoshijia-iota.vercel.app/)

访问线上版本后，可以注册测试账号体验主要功能。

> 如果线上版本正在部署或 Firebase 配置调整中，可能需要稍后刷新重试。

## 核心功能

- 仪表盘任务面板：展示今日进度、今日任务、完成度和连续记录天数，把记录入口收敛为称重、饮食、运动和复盘任务。
- 体重记录与目标管理：记录每日体重，展示今日/最新体重、初始体重、目标体重和剩余差距。
- 身体概览：基于身高、年龄、性别、活动水平和最近体重展示 BMI 记录参考。
- 围度记录：记录腰围、臀围、大腿围、上臂围，支持趋势查看、最近有效记录展示和今日记录删除。
- 饮食记录：按早餐、午餐、晚餐和加餐记录食物、份量、饥饿程度、触发原因和情绪状态。
- 运动记录：支持分钟、步数、公里、次数、组数、圈数和自定义单位。
- 日复盘与周复盘：根据饮食、体重、围度和运动记录生成执行度、高风险场景和下周策略。
- 食材库存与菜谱生成：管理食材库存和可用顿数，并按库存、顿数和基础搭配规则生成一周三餐。
- 轻量营养参考：根据身体资料估算热量和三大营养素参考目标，并为菜谱提供每餐份量建议。
- 菜谱导出：支持将当前一周菜谱通过浏览器打印或保存为 PDF。
- 账号数据管理：支持一键清除历史数据但保留账号，也支持注销账号并删除用户数据。

## 产品思路

好食家的核心不是简单记录，而是建立一个轻量闭环：

```text
记录数据 -> 识别模式 -> 生成复盘 -> 调整下一步计划
```

因此项目里有几类设计取舍：

- 把目标设定合并进体重记录页，减少在相关任务之间来回切换。
- 把食材库存和菜谱生成合并到同一页面，让菜谱直接读取最新库存。
- 菜谱生成只使用用户录入的食材，库存不足时不强行生成完整菜单，而是提示需要补充的食材类别。
- 用“可用顿数”表达食材计划吃几顿，生成菜谱时按顿数消耗库存，减少同一食材被无限重复安排。
- 把仪表盘改为“今日进度 + 今日任务”，减少重复入口，让用户先看当前状态，再补记录。
- 复盘页面独立出来，让“看见问题”和“形成下一步策略”成为核心入口。
- 提供清除历史数据和注销账号能力，让个人数据管理保持可控。
- 移动端记录流程尽量压缩操作成本，因为体重、饮食和运动记录通常发生在碎片场景。

## 技术栈

- Next.js 16
- React 19
- TypeScript
- Tailwind CSS v4
- shadcn/ui
- Firebase Authentication
- Cloud Firestore
- Vercel

## 技术实现要点

- 使用 Firebase Auth 管理邮箱密码登录和登录态保护。
- 注销账号前使用 Firebase Auth 重新认证，避免登录态过期或误操作。
- 使用 Firestore 子集合存储每日体重、围度、饮食和运动记录。
- 提供用户历史数据清理能力，可删除记录、计划、食材、体重目标和菜谱偏好。
- 仪表盘读取最近记录状态，计算今日任务完成度和连续记录天数。
- 将日复盘、周复盘分析逻辑抽象到独立模块，供饮食页和复盘页复用。
- 在食材与菜谱页面中使用同一份菜单数据驱动表格、九宫格和日历三种视图，并结合可用顿数约束库存消耗。
- 根据用户身体资料和最近体重即时估算轻量营养参考目标，不持久化医疗级结论。
- 使用打印专用样式控制菜谱导出范围，隐藏导航、按钮和表单，只保留当前菜单内容。
- 保留旧路由兼容跳转，避免页面合并后出现旧入口 404。
- 全局视觉采用纸白网格底、炭黑主色和少量高饱和状态色，强化数据游戏化记录体验。

## 本地运行

```bash
npm install
npm run dev
```

打开浏览器访问：

```text
http://localhost:3000
```

项目依赖 Firebase。若要完整运行登录、注册和数据读写功能，需要配置 Firebase 项目，并在 `src/lib/firebase.ts` 中使用对应的客户端配置。

## 项目状态

这是一个个人使用中的减脂记录与复盘网页应用，目前仍在持续迭代中。项目已实现核心记录、复盘、食材库存、菜谱生成、轻量营养参考、账号数据管理和仪表盘任务面板，后续会继续根据真实使用体验优化移动端记录效率、复盘分析质量和页面细节。

## 后续计划

- 优化移动端高频记录体验。
- 继续改进复盘分析规则，让输出建议更具体。
- 继续细化页面局部视觉层级和交互反馈。
- 增强菜谱生成对保质期、个人偏好和长期记录的利用。
- 补充更系统的测试和错误状态处理。
